import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        mode: 'dark',
        background: {
            default: '#303030',
        },
        primary: {
            main: '#3B84D9',
        },
        secondary: {
            main: '#3347ce',
        },
    },
});

export default theme;
